version: '1.0'
steps:
  cf-cli-step:
    image: codefreshio/cf-cli:latest
    commands:
        - node index.js login --token ${{TOKEN}} -u ${{USER}}
        - node index.js builds build -a ${{ACCOUNT}} -o ${{REPO_OWNER}} -r ${{REPO_NAME}} --branch ${{BRANCH}} --pipelineName ${{PIPELINE_NAME}}